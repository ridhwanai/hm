[package]
name = "azenith-profilesettings"
version = "1.0.0"
edition = "2024"
description = "AZenith Profiler binaries"

[dependencies]
glob = "0.3.1"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = true